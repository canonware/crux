# Test canonization.

import random

# Re-seed the PRNG so that test results are repeatable.
random.seed(42)

print "Test begin"

for i in xrange(10):
    for j in xrange(4):
        t = Crux.Tree.Tree(i)
        print "==="
        print "Original: %s" % t.render()
        t.canonize()
        print "Rooted:   %s" % t.render()
        t.deroot()
        t.canonize()
        print "Unrooted: %s" % t.render()

print "Test end"
